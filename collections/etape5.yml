info:
  name: etape5
  type: http
  seq: 5

http:
  method: POST
  url: 172.16.3.254:8001/5-content-type
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
